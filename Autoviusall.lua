local BallVelocity = Ball.Velocity

        local BallSpeedMagnitude = BallVelocity.Magnitude

        local function GetParryDistance(BallSpeedMagnitude)
end

        local function GetParryDistance(BallSpeedMagnitude)
local baseDistance = 10 -- it's base distance, used for lowest ball's speed
end

        local function GetParryDistance(BallSpeedMagnitude)
local baseDistance = 10 -- Like I said earlier
local speedFactor = 0.3 -- The number we're gonna multiply on
end

        local function GetParryDistance(BallSpeedMagnitude)
local baseDistance = 10 -- Number
local speedFactor = 0.3 -- Multiply
return baseDistance + (BallSpeedMagnitude * speedFactor) -- BallSpeedMagnitude is ball's speeed, and we multiply it on it
end
